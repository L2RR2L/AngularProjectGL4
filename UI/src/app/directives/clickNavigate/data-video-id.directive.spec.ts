import { DataVideoIdDirective } from './data-video-id.directive';

describe('DataVideoIdDirective', () => {
  it('should create an instance', () => {
    const directive = new DataVideoIdDirective();
    expect(directive).toBeTruthy();
  });
});
