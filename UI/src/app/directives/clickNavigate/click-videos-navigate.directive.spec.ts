import { ClickVideosNavigateDirective } from './click-videos-navigate.directive';

describe('ClickNavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickVideosNavigateDirective();
    expect(directive).toBeTruthy();
  });
});
